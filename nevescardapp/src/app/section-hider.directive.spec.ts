import { SectionHiderDirective } from './section-hider.directive';

describe('SectionHiderDirective', () => {
  it('should create an instance', () => {
    const directive = new SectionHiderDirective();
    expect(directive).toBeTruthy();
  });
});
